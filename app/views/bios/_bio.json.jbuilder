json.id bio.id
json.name bio.name
json.bodytext bio.bodytext
json.github bio.github
json.created_at bio.created_at
json.updated_at bio.updated_at

# json.extract! bio, :id, :name, :bodytext, :github, :created_at, :updated_at
# json.url bio_url(bio, format: :json)
