json.id link.id
json.title link.title
json.link link.link
json.created_at link.created_at
json.updated_at json.updated_at

# json.extract! link, :id, :title, :link, :created_at, :updated_at
# json.url link_url(link, format: :json)
